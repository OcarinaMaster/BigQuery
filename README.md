A code segment that pulls queries from google bigquery. Can change the query being pulled by manipulating the SQL inside the code segment

Streamlit based web app:
URL: https://salesviewer.streamlit.app/
